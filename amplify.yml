
version: 1
frontend:
  phases:
    build:
      commands:
        # Add any existing build commands here
        - npm run build
  artifacts:
    # Adjust the paths if needed to match the location of your JSX files
    baseDirectory: build
    files:
      - '**/*'
